/* ===================================================================
   Supabase 설정
   ------------------------------------------------------------------
   Supabase 프로젝트를 만든 뒤, 아래 3개 값을 실제 값으로 바꿔주세요.

   1) SUPABASE_URL, SUPABASE_ANON_KEY
      → Supabase 대시보드 → 좌측 Project Settings → API 에서 확인

   2) ADMIN_EMAIL
      → 관리자 페이지(admin.html) 접근을 허용할 본인의 GitHub 이메일 주소
      → Supabase 테이블의 RLS 정책에 설정한 이메일과 반드시 동일해야 합니다.
   =================================================================== */

window.SUPABASE_URL = "https://YOUR_PROJECT_REF.supabase.co";
window.SUPABASE_ANON_KEY = "YOUR_ANON_PUBLIC_KEY";
window.ADMIN_EMAIL = "your-github-email@example.com";
